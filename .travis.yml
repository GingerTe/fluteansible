---
language: python
python: "2.7"

before_install:
  # Make sure everything's up to date.
  - sudo apt-get update -qq

install:
  - apt-get install docker
  # Install Ansible.
  - pip install ansible
  - pip install molecule


script:
  - molecule test
